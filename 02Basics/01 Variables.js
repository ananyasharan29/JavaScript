//Reserving space in memory means declaring variables

// string, boolean values, number and undefined are some of the data types.

// const declares variable as constant (i.e., constant variable) and it cannot be
// reassigned. Helps in keeping the data in the variable unique.

var fullName = "Ananya Sharan";

//how to name variable

// fullName
// full_name
// full-name
// fullName

var courseName = "JavaScript";

var isLoggedIn = false;

var loggedCount = 30;

// console.log(loggedCount);
// console.log("loggedCount");
// console.log(courseName);

var paymentMode;

// console.log(paymentMode);
paymentMode = "Credit Card";
console.log(paymentMode);

